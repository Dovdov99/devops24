---
- name: Use the custom anagrammer module
  hosts: localhost
  gather_facts: false
  vars:
    message: "sirap i paris"
  tasks:

    - name: Run anagrammer module
      anagrammer:
        message: "{{ message }}"
      register: result

    - name: Show result
      ansible.builtin.debug:
        var: result
